#!/bin/bash
# SMS Conductor Database Viewer - Mac Double-Click Launcher
# This file can be double-clicked from Finder

# Get the directory where this script is located
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
cd "$DIR"

# Run the launcher
./run_mac.sh

